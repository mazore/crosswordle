TODO:
- Shared tile selection quirks
    - If word is selected and selected a shared tile in that word, never switch word selection
    - Double tap shared tile to switch to second word in wordGrid
- Backspace moves selection back (only if on blank tile)
- Enter key functionality, that colors (and flips) each tile
- On screen Keyboard class linked up to keypress enter and backspace methods
- Mobile compatibility
- Methods for Game to color keyboard buttons
